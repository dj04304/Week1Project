namespace _3.DataType
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //자료형과 변수

            // 변수명 주의사항
            // 1. 이미 C#에서 사용되는 클래스 내지 메소드 변수등이 존재하기 때문에 (이를 keywords 라고 한다.) 이를 사용하지않는다.
            // 2. 첫 문자는 언더스코어(_) 또는 알파벳이어야 한다.
            // 3. 대소문자를 구분하고, 두번째부터는 알파벳, 언더스코어, 숫자가 올 수 있다.

            int num = 10;
            float f = 3.14f;
            char c = 'A';
            string str = "Hello World";

        }
    }
}